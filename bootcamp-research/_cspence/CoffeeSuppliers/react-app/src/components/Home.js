import React from 'react';

// TODO : Categories Bar on top + pagination
// TODO : Products below (paying attention to the selected category)
// TODO : Search Bar to search for product
const Home = (props) => {
    return (
        <>
        </>
    );
}

export default Home;
